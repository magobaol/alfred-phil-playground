{
  "name": "alfred-phil",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "webpack --config webpack.dev.js",
    "release": "npm run clean-release && webpack --config webpack.prod.js && cp workflow/info.plist workflow/icon.png dist/ && zip dist/alfred-phil.alfredWorkflow dist/*",
    "clean-release": "rm -rf dist",
    "clean-build": "rm -rf workflow/*.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.15.3",
    "path": "^0.12.7",
    "ts-loader": "^9.4.2",
    "webpack": "^5.76.2",
    "webpack-cli": "^5.0.1"
  },
  "dependencies": {
    "@magobaol/phil": "^1.0.1",
    "commander": "^10.0.0"
  }
}
